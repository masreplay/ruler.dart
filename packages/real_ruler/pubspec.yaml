name: real_ruler
description: A ruler is a widget that displays a ruler with a given distance in cm or inches
version: 0.0.2
repository: https://github.com/masreplay/error_handler.dart
homepage: https://github.com/masreplay/error_handler.dart


environment:
  sdk: ">=2.19.2 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  freezed_annotation: ^2.2.0
  json_annotation: ^4.8.0
  device_info_plus: ^8.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

  build_runner: ^2.3.3
  freezed: ^2.3.2
  json_serializable: ^6.6.1

flutter:
